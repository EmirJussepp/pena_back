ktor {
  application {
    modules = [ com.example.ApplicationKt.module ]
  }

  deployment {
    host = "0.0.0.0"
    port = ${?PORT}   # si existe, lo usa
    port = 8080       # fallback local
  }
}
